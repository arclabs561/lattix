# lattix

Knowledge graph substrate: core types + basic algorithms + formats.

This crate is the **facade** for `lattix-core` (the implementation crate). It is intentionally
minimal by default; enable features for formats/algorithms as needed.

## License

MIT OR Apache-2.0
