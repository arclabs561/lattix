# lattix-core

Graph/KG substrate: triples + PageRank + random walks.

This is the core implementation crate in the `lattix/` repo. Most users should depend on the
facade crate `lattix` instead (minimal-by-default surface with feature-gated extras).

## License

MIT OR Apache-2.0
